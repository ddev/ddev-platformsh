name: ddev-platformsh

pre_install_actions:
  - |
    #ddev-nodisplay
    ddev debug capabilities | grep multiple-dockerfiles || echo "Please upgrade ddev to v1.19.3+ to support multiple Dockerfiles" && exit 3

project_files:
  - docker-compose.platformsh.yaml
  - web-build/Dockerfile.platformsh

global_files:
  - commands/web/platform

post_install_actions:

